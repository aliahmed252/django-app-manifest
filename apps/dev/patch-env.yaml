apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: malerator-app
spec:
  template:
    spec:
      containers:
        - name: django-app
          env:
            - name: DEBUG
              value: "true"
