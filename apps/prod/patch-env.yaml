apiVersion: apps/v1
kind: Deployment
metadata:
  name: malerator-app
spec:
  template:
    spec:
      containers:
        - name: django-app
          env:
            - name: DEBUG
              value: "false"
